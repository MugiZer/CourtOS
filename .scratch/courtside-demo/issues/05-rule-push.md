Type: task
Status: open
Blocked by: 04

## Question

Promote the minimal versioned setRules flow with exactly 3 controls + one Apply button. NO-AD SCORING ON/OFF, DECIDING SET Full Set / 10-Point Match Tiebreak (a format choice, not ON/OFF), CHANGEOVER TIMER 90s / 60s. Organizer hits Apply to next games. CourtGuard validates the patch and activates it at the NEXT_GAME boundary so a live game never breaks; court + dashboard agree on pending vs active version.

Done when: the organizer changes a rule once, target courts visibly receive the new ruleset version, and court + dashboard agree on when it becomes active.
